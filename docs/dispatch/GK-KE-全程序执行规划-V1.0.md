# GK-KE 全程序执行规划（L0-2 已激活 → L6）

> 角色：Tech Lead（planning_review，总指挥）｜日期：2026-09-12｜基线 HEAD：`dda067c`
> 权威依据：`docs/dispatch/GK-KE-L0-2-开工规划.md` 依赖图 + `C08 §2` 实施顺序 + `C08 §5` 阶段授权
> 授权依据：`GK-KE-OWNER-003` §9「范围不变的客观落实不再申请相同 Owner 决定」
> 状态：**L0-2 已关闭并激活**（OC-01 CLOSED / ACT-01 EFFECTIVE）

---

## 一、权威依赖图（原文照录，不改编）

```
L0-2 契约激活 ──┬──> L1-1 公共语义 ──> L2-1 语义查询 ──> L4-1 地图激活 ──> L4-2 GITS 闭环
                │                                            ↑
                ├──> L1-2 模拟源 ─────────────┘（模拟数据支撑 L2-1 复算）
                │
                ├──> L2-2 注册中心 ──> L3-1 工厂与候选 ──> L3-2 审核发布 ──> L4-1（地图引用已发布资产）
                │
                └──> L5-1 Kuzu / L5-2 LightRAG（可关，不阻塞 L4 基础闭环）
                                                          └──> L6 运行验收（依赖全部）
```

**六条依赖规则**（`开工规划.md` §二）：
1. **L0-2 是总前提** —— 已满足 ✅
2. **L1-1 → L2-1 → L4-1 → L4-2**：主业务链
3. **L1-2 → L2-1**：模拟数据支撑指标复算（C001 = 2,983,333.33）
4. **L2-2 → L3-1 → L3-2**：知识加工链
5. **L3-2 → L4-1**：地图激活需已发布资产
6. **L5-1/L5-2 不阻塞 L4**：图/RAG 是可关增强项

**关键交点（易错）**：
- `L2-1` 需 **L1-1 + L1-2 双前置**（不是只等 L1-1）
- `L4-1` 需 **L2-1 + L3-2 双前置**（指标 + 已发布资产）
- `L4-2` 需 **L4-1 + L2-1 + L3-2 三前置**

---

## 二、五波（Wave）编排总览

| Wave | 工作单 | 并行度 | 前置 | 角色 | 当前状态 |
|---|---|---|---|---|---|
| **A** | L0-2 契约激活 | 串行（唯一入口） | L0-1 | TL+FP+QA | ✅ **完成并关闭** |
| **B** | L1-1 / L1-2 / L2-2 | **三线并行** | A | FP | 🟢 **可立即启动** |
| **C** | L2-1 / L3-1 / L5-1+L5-2 | **三线并行** | B（部分） | FP | ⏳ 待 B 部分完成 |
| **D** | L3-2 / L4-1 | 部分并行 | C | FP | ⏳ |
| **E** | L4-2 GITS 闭环 | 串行 | D+L2-1+L3-2 | FP+业务专家 | ⏳ |
| **F** | L6 运行验收 | 串行收尾 | 全部 | E2E Owner+QA | ⏳ |

> **修正说明**：`开工规划.md` 原 Wave 分组把 L2-1/L3-1/L3-2 等归入 Wave C/D；
> 本规划按**真实依赖**细化为 A–F 六波，并在 §三给出**并行度最大化**的排程。
> 这不改变任何依赖关系，只调整批次边界以提升并行度。

---

## 三、完整可执行任务清单（含并行/串行判定）

### 3.1 立即可执行：Wave B（三线并行，零前置阻塞）

| 线 | Loop ID（建议） | 工作单 | C 合同 | 退出标准 | 角色 |
|---|---|---|---|---|---|
| **B1** | `GK2-l1-1-public-semantics` | **L1-1 公共语义**：12 个最小类型/关系与域扩展；双版本引用；SemanticPackage、ID/时间/金额规范 | C01 | 同名异义、破坏变更、身份误并测试通过；**语义专家审** | FP + 语义专家 |
| **B2** | `GK3-l1-2-simulation-source` | **L1-2 模拟源**：导入随包种子；实现模拟对象服务和账务快照；模拟 API、manifest、数据报告 | C07 | 外键/余额/币种/重复负例通过；**数据专家核对** | FP + 数据专家 |
| **B3** | `GK4-l2-2-registry` | **L2-2 注册中心**：稳定 ID、版本、外部引用、权限发现、能力探针；Registry API、资产导入/差异工具 | C02 | 非授权目录无泄漏；断链拒绝；**并发更新 CAS 测试** | FP + QA |

**并行可行性**：B1（语义）与 B2（模拟源）**无直接依赖**；B3（注册中心）**完全独立**。
三线可同批启动，互不阻塞。

---

### 3.2 解锁后可执行：Wave C（三线并行）

| 线 | Loop ID（建议） | 工作单 | 前置 | 退出标准 | 角色 |
|---|---|---|---|---|---|
| **C1** | `GK5-l2-1-semantic-query` | **L2-1 语义查询**：指标定义与审批；白名单编译器；范围过滤；MetricDefinition、QueryPlan、复算报告 | **B1 + B2** | 日均/粒度/缺失/跨币/越权测试通过；**指标 Owner 签署（OM-C1/C2）**；关闭 **OC-02** | FP + 指标 Owner |
| **C2** | `GK6-l3-1-factory-candidate` | **L3-1 工厂与候选**：原文/片段/断言流水线；候选图；去重/冲突；可恢复 ingestion job、ReviewPackage | **B3** | **18 文档全可回链**；注入/同名/时态负例保留 | FP |
| **C3** | `GK7-l5-1-kuzu` | **L5-1 Kuzu**：锁版、图投影、有界 queryId、候选/发布隔离；GraphQueryPort、重建/导出/恢复报告 | 无硬依赖 🔓 | 不覆盖源；边级权限/撤销；**停止图服务的降级用例** | FP |
| **C4** | `GK8-l5-2-lightrag` | **L5-2 LightRAG**：候选抽取/自定义图试验；A/B/C 对照；固定版本 Spike、逐题结果、成本报告 | 无硬依赖 🔓 | **满足 C05 门槛才启用**；不满足保留基础路线 | FP |

> **C3/C4 的"无硬依赖"**：依赖图注明"可关，不阻塞 L4 基础闭环"。
> 但**实践建议**：等 B 线开始后再启动，避免与主链抢 reviewer 带宽。
> 若追求极限并行，C3/C4 **理论上现在即可启动**（Owner 已在 `OWNER-003` §6.2 延续 L5 授权）。

---

### 3.3 Wave D（双线并行）

| 线 | Loop ID（建议） | 工作单 | 前置 | 退出标准 | 角色 |
|---|---|---|---|---|---|
| **D1** | `GK9-l3-2-review-release` | **L3-2 审核发布**：双维审核、hash 绑定、分用途发布、撤销；Review UI/API、Release Manager | **C2（L3-1）** | 人工更正可追踪；**审批后改字阻断**；半发布/撤销测试；关闭 **OC-03**（发布前） | FP + 知识 Owner |
| **D2** | `GK10-l4-1-map-activation` | **L4-1 地图激活**：TaskTemplate、MapSpec、确定性路由/计划；一条经营任务地图、计划与证据接口 | **C1（L2-1）+ D1（L3-2）** | **同输入同 hash**；同优先级歧义拒绝；必需能力缺失拒绝；关闭 **OC-04** | FP + QA |

> **注意**：D2 需 D1 完成（依赖规则 5），**故 D1→D2 串行**；D1 与 Wave C 的 C3/C4 可并行。

---

### 3.4 Wave E（串行）

| Loop ID（建议） | 工作单 | 前置 | 退出标准 | 角色 |
|---|---|---|---|---|
| `GK11-l4-2-gits-closed-loop` | **L4-2 GITS 闭环**：解读→体检→推荐→确认→模拟跟进动作；任务界面/接口、回执与恢复 | **D2（L4-1）+ C1（L2-1）+ D1（L3-2）** | 3000 万场景、UNKNOWN、失效版本、**超时对账**；**业务专家验收（OB-C3）**；关闭 **OC-05** | FP + 业务专家 |

---

### 3.5 Wave F（串行收尾）

| Loop ID（建议） | 工作单 | 前置 | 退出标准 | 角色 |
|---|---|---|---|---|
| `GK12-l6-runtime-acceptance` | **L6 运行验收**：并发、故障注入、升级/恢复、保留删除、完整追踪；独立 QA 包、操作说明、部署锁文件 | **全部** | 无未关闭 BLOCKER/MAJOR；**Owner 另行裁定试点范围**；关闭 **OC-06** | E2E Owner + 独立 QA |

---

## 四、跨仓并行任务（KERT 侧，与主仓无阻塞耦合）

`GK-KE-OWNER-003` §7.1 + §9 要求，以下**必须由 KERT 维护方**执行（本环境对 KERT 仓只读）：

| # | 任务 | 依据 | 与主仓关系 | 状态 |
|---|---|---|---|---|
| **K-1** | 在 `Leibniz-KERT/docs/integration/L0-2_G2_MAPPING_CONFIRMATION.md` 落盘正式副本，引用主仓定义（不复制） | OWNER-003 §7.1/§9 | **与 Wave B 并行，不阻塞** | ⏳ 待 KERT |
| **K-2** | 登记三个目标的唯一受控路径 + 内容 SHA-256 + 落实提交 | OWNER-003 §7.1 | 同上 | ⏳ 待 KERT |
| **K-3** | 检查 KERT 权威索引是否已把某定义指定到其他路径；若是保留其唯一源 | OWNER-003 §9 | 同上 | ⏳ 待 KERT |
| **K-4** | KERT 侧 `SIM-ROUTE-001` / `SIM-MAP-FINANCE` 运行适配（L4-1 阶段） | OWNER-003 §7.3 | 随 D2 推进 | ⏳ |
| **K-5** | 产品卡集合索引的**版本覆盖风险**核验（`{productId: card}` 同 ID 多版本会覆盖） | OWNER-003 §3.2 | **与 B3（注册中心 L2-2）相关** | ⏳ |
| **K-6** | KERT 侧 `ActivationPlan` 规划/运行集成 | OWNER-003 §6 | 随 D2 推进 | ⏳ |

> **K-1~K-3 不阻塞主仓任何 Wave** —— 主仓三份定义已落盘并指纹化，KERT 侧只需引用。
> **K-5 建议在 B3 前完成**（若 KERT 输入不保证每产品单一版本，B3 需在建索引前拒绝混版）。

---

## 五、OC 条件台账（六个，逐 Loop 承接）

| 条件 | 承接 | 关闭时点 | 当前 |
|---|---|---|---|
| **OC-01** 版本和对象对应 | L0-2 | L0-2 激活前 | ✅ **已关闭** |
| OC-02 指标服务化 | L2-1 | L2-1 退出前 | ⏳ Wave C |
| OC-03 候选内容与发布 | L3-2 | L3-2 发布前 | ⏳ Wave D |
| OC-04 地图和能力 | L4-1 | L4-1 激活/退出 | ⏳ Wave D |
| OC-05 完整经营闭环 | L4-2 | L4-2 退出前 | ⏳ Wave E |
| OC-06 范围持续遵守 | **每 Loop** | L6 放行前 | 🔄 持续 |

---

## 六、并行度排程（关键路径分析）

### 6.1 关键路径（最长链）

```
L0-2 → L1-1 → L2-1 → L4-1 → L4-2 → L6
  ✅    (B1)   (C1)   (D2)   (E)    (F)
```

**关键路径上的 5 个 Loop**：`B1(L1-1) → C1(L2-1) → D2(L4-1) → E(L4-2) → F(L6)`
其余全部可挂到旁路并行 —— 这是排程优化的核心。

### 6.2 推荐批次（最大化并行）

| 批次 | 并行工作单 | 说明 |
|---|---|---|
| **批 1** | `B1(L1-1)` ‖ `B2(L1-2)` ‖ `B3(L2-2)` | 三线并行，零阻塞 |
| **批 2** | `C1(L2-1)` ‖ `C2(L3-1)` ‖ `C3+C4(L5)` | B1+B2 完成即起 C1；B3 完成即起 C2 |
| **批 3** | `D1(L3-2)` ‖ （C3/C4 若未完成，继续） | D1 完成后才能起 D2 |
| **批 4** | `D2(L4-1)` | 需 C1+D1 |
| **批 5** | `E(L4-2)` | 串行 |
| **批 6** | `F(L6)` | 串行收尾 |

### 6.3 并行度总结

| 波 | 最大并行数 | 串行点 |
|---|---|---|
| Wave B | **3** | 无 |
| Wave C | **3~4** | L2-1 需双前置 |
| Wave D | **2**（D1 与 C3/C4）→ 1（D2） | **D1→D2 必须串行** |
| Wave E | 1 | — |
| Wave F | 1 | — |
| **KERT 跨仓** | **与主仓全程并行** | 无阻塞耦合 |

---

## 七、不可并行（硬串行）清单

以下**必须串行**，违反即为依赖错误：

| # | 关系 | 原因 |
|---|---|---|
| 1 | `L0-2 → 一切` | 唯一入口（已完成） |
| 2 | `L1-1 → L2-1` | 语义类型是查询的前提 |
| 3 | `L1-2 → L2-1` | 模拟数据是复算的前提 |
| 4 | `L2-2 → L3-1` | 注册中心是工厂的前提 |
| 5 | `L3-1 → L3-2` | 候选是审核的前提 |
| 6 | **`L3-2 → L4-1`** | 地图引用已发布资产 |
| 7 | **`L2-1 → L4-1`** | 地图引用指标 |
| 8 | `L4-1 → L4-2` | 闭环需地图 |
| 9 | `L4-2 → L6` | 验收需全部 |

**可并行（易误判为串行）**：

| # | 关系 | 说明 |
|---|---|---|
| 1 | `L1-1` ‖ `L1-2` | 语义 vs 模拟源无直接依赖 |
| 2 | `L2-2` ‖ `L1-1`/`L1-2` | 注册中心完全独立 |
| 3 | `L3-1` ‖ `L5-1`/`L5-2` | 知识加工 vs 图/RAG 增强 |
| 4 | `L3-2` ‖ `L5-1`/`L5-2` | 审核发布 vs 图/RAG（图不阻塞 L4） |
| 5 | **`L5-1`/`L5-2` ‖ 主链全程** | 可关增强项，不阻塞 L4 基础闭环 |

---

## 八、角色分工与门禁纪律（每个 Loop 统一）

| 环节 | 角色 | 可记录 |
|---|---|---|
| 合同变更 | 合同 Owner（不可由 TL 代签） | 决议留痕 |
| 实现 | Feature Pilot | **仅 `DEV_SELF_CHECK_PASS`** |
| 冲突审计 | 独立 QA | **仅 `QA_PASS`** |
| 业务/领域验收 | 语义专家 / 数据专家 / 指标 Owner / 知识 Owner / 业务专家 | Owner 决议 |

**每 Loop 统一纪律**：
1. 合同先行：改 `specs/` → `make generate` → `make check` → 写实现
2. 禁改 `generated/`；禁 `git add .`；禁启用隔离资产
3. 失败先写 Loop `FAILURES.md` 再修复
4. 每 Loop 用 `make new-loop` 创建，绑定对应 C 合同
5. 三检（loop_guard / memory-check / evidence-check）开场与收工各一次

---

## 九、当前可立即启动的建议批次

**批 1（Wave B，三线并行）** —— 零阻塞，建议即刻启动：

| 优先级 | Loop | 理由 |
|---|---|---|
| **P0** | `B1(L1-1 公共语义)` | 关键路径起点，越早越好 |
| **P0** | `B2(L1-2 模拟源)` | L2-1 双前置之一，与 B1 并行 |
| **P1** | `B3(L2-2 注册中心)` | 独立，且是 L3-1 前置 |

**并行推进的跨仓任务**：`K-5`（产品卡版本覆盖风险核验）—— 与 B3 直接相关，建议同步发起。
