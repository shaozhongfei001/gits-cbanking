# 自查方案 · 逐 `#` 子项**源代码举证表** V1.0

> 出具：GK-KE 全局 Tech Lead｜日期：2026-09-13
> 起因：Owner 指出「**到目前为止你还没有举证逐项核对源代码已经改正的**」
> **本表对 §一 / §三 / §四 / §五 的每一个 `#` 子项，给出源代码或制品的直接证据，并标注是否已整改。**
> 纪律：**无证据者标「未举证」，不得含混。**
> 基准文件：`docs/architecture/GK-KE-自查盘点与防复发方案-V1.0.md`

---

## §一 按失效机制分类（6 个子项）

| `#` | 子项 | **源代码/制品证据** | 已整改？ |
|---|---|---|---|
| **A** | **断言取代核对**（10 条） | **实例**：我宣布"缺 workspace"时，实际 `bank_front_ws` **存在**于 `/home/szf/dev/Leibniz-KERT/bank_front_ws`（6.7M，含 `02_work/03_core/04_serve/90_control`）。我只查了脚本 `DEFAULT_WS` 算出的路径。**残留检验**：`FAIL-33` 的 37/55 错误行号 → 现由 `scripts/gk_ke_criteria_line_audit.py` **机械校验**，实测 55/55 通过 | ⚠️ **部分**（有机械防线，但"穷尽检索凭证" C1 **未建**） |
| **B** | **检查器失效**（13 条） | **实例**：`scripts/gate_selftest.py` 建成前，`grep -c "反例\|注入"` 在 `run_gates.py` / `gk_ke_criteria_line_audit.py` 中命中 **0**；实测 **18 门禁 0 个**有可复现负例测试。**现状**：`scripts/gate_injection_tests.py` 已建，**9 项注入被检出** | ⚠️ **部分**（9/18 有证据，见下） |
| **C** | **无反驳检验的设计**（5 条） | **实例**：`FAIL-38` 的恒真 S2 仍在 `GK-KE-语义级消费验证方案-V1.2.2.md`（**判据内容非我职责，未改**）。**但反例要求已入制度**：`gate_selftest.py` 的 CASES 中"散文含词不应命中"即一条反例用例 | ⚠️ **部分**（机制已建，语料本身未重写） |
| **D** | **边界越界**（1 条） | **实例**：`FAIL-42` —— `gk_ke_criteria_line_audit.py --fix` 曾改写判据文档 5 行。**现状**：该脚本仍带 `--fix`；**限制为仅作者可用的改动尚未实施** | ❌ **未整改** |
| **E** | **手工编辑失误**（2 条） | **实例**：本轮 `gate_injection_tests.py` 的 `finally` 顺序 bug 又致受保护制品被写坏（详见 §四 C7）。**现状**：改用"按语义精确注入 + 只读即跳过"，并加 `ast.parse` 自查 | ⚠️ **部分**（新增了还原校验，但同类错误本轮仍在发生） |
| **F** | **其他**（8 条） | `FAIL-04/05/07/08/09/13/16/18` 为独立缺陷，**与上述机制无关**，无系统性控制对应 | ❌ **未系统整改** |

**§一小结**：6 项中 **0 项完全整改**，**4 项部分**，**2 项未整改**。

---

## §三 为什么"更小心"无效（4 个证据行）

| `#` | 断言 | **源代码/制品证据** | 是否成立 |
|---|---|---|---|
| 1 | 同类错误**复发 4 次** | `FAILURES.md` 中静默跳过类：**22 → 36 → 40 → 41**（`grep '^## FAIL-'` 可核）。**本轮又 +1**：`gate_injection_tests.py` 的 `finally` 顺序 bug 属同类 | ✅ **成立（且已超 4 次）** |
| 2 | 复发**间隔缩短**至同轮内 | `FAIL-29`（声称修好 §4 审计覆盖）与 `FAIL-32`（**第二次**犯同一错）在同一天、**间隔不到一轮** | ✅ **成立** |
| 3 | "声称已修好" **2 次** | `FAIL-29`、`FAIL-32`；**本轮新增 `FAIL-49`**：5/13 注入无效却差点报成"门禁弱" | ✅ **成立（已 3 次）** |
| 4 | 独立复核**3 轮全部由他人发现** | `INDEPENDENT-JUDGEMENT-S1-S5-V1.0.md`（第 1 轮）、`GK-KE-判据V1.2.0独立复核记录与处置-V1.0.md`（第 2 轮）、`GK-KE-第三四次独立复核记录与测量面决策简报-V1.0.md`（第 3/4 轮）。**均非我发现** | ✅ **成立** |

**§三小结**：4 项断言 **全部成立**，且其中 3 项**在本轮被再次印证**（复发、声称已修、他人发现）。

---

## §四 方案 C1–C7（7 个子项）

| `#` | 控制项 | **是否已建成制品** | 证据 / 缺口 |
|---|---|---|---|
| **C1** | 断言必须附**穷尽检索凭证**（范围+检索式+命中数） | ❌ **未建** | 无脚本、无模板。本轮仍靠人手写叙述 |
| **C2** | 检查器必须**自证覆盖**（发现数 == 检查数） | ⚠️ **部分** | `gk_ke_criteria_line_audit.py` 已实现计数守恒（实测输出"引用总数 62 / 已核对 61 / 未归属 0"）；`gk_ke_criteria_key_audit.py` 有 `MIN_EXPECTED_KEYS` 下限。**其余门禁未见同类实现** |
| **C3** | **无负例测试的门禁，PASS 不予采信** | ⚠️ **部分** | `gate_selftest.py`（10/10 分类用例）+ `gate_injection_tests.py`（**9 项注入被检出**）。**但 18 门禁中仅 9 个有证据** |
| **C4** | "已修复"必须附**回读证据**（前后对比） | ❌ **未建** | 无模板、无检查。`FAIL-49` 即是"声称修好但未验证"的又一次 |
| **C5** | 变更必须做**反向检索** | ❌ **未建** | 无工具。`FAIL-30/34/37` 均因此类遗漏 |
| **C6** | 合同变更**三问**（谁生产/谁强制/无条件还是有条件） | ❌ **未建** | `FAIL-44`（我改合同未查强制方 → 技能 1/4 概率失败）后**仅写了教训，未建检查** |
| **C7** | **工具代码与制品同权** | ⚠️ **部分** | `--fix` 限制**未实施**（同 §一 D）；但本轮新增：注入前查可写性、`finally` 包裹、还原后**校验哈希与权限** |

**§四小结**：7 项中 **0 项完全建成**，**3 项部分**，**4 项未建**。

---

## §五 可信度分级（5 个子项）

| `#` | 原分级 | **按本轮源代码证据的复核结论** |
|---|---|---|
| 1 | ✅ 可采信：**两个判据审计** | **更正为：现可采信** —— 但**原因与原文不同**。原文称"各有负例测试"，实测**当时两者都没有**（那些注入是未落盘的 shell 操作）。**现在**才真正有了：`gate_injection_tests.py` 对两者各注入真实缺陷，**均被检出（FAIL）** |
| 2 | ⚠️ 条件采信：机械数据 | **维持**。但须补：机械数据**若来自无负例测试的门禁**（18 中的 9 个），其 PASS **不在可采信范围** |
| 3 | ❌ 不可采信：我的**叙述性结论** | **维持，且加重** —— 本轮新增三例：`FAIL-45`（可信度分级的依据本身不可验证）、`FAIL-46`（根因误诊且是我造成）、`FAIL-49`（负例测试本身无效） |
| 4 | ❌ 不可采信：**我对自己产出的"是否达成"判定** | **维持**（三轮独立复核全部由他人发现） |
| 5 | ❌ 不可采信：**无负例测试的门禁 PASS** | **维持，但范围已缩小**：从 18/18 不可采信 → **9/18 有证据、9/18 仍不可采信** |

**§五小结**：5 项中 **1 项更正（1）**，**4 项维持且 3 项加重**。

---

## 附：本轮门禁负例测试覆盖（`make gate-injection-tests` 实测）

| 状态 | 数量 | 门禁 |
|---|---|---|
| ✅ **注入被检出** | **9** | contract-examples · contract-coverage · metric-definitions · product-card · registry-contract · plan-compiler · **secret-scan** · **criteria-key-audit** · **criteria-line-audit** |
| ?? 注入有效性未确立 | 3 | dataset-v2 · acceptance-pack · loop-guard |
| SKIP 目标只读（按纪律不放开写位） | 1 | semantic-rule-gate |
| UNCOVERED 未设计注入 | 7 | contract-check · enum-consistency · probe-mutation-tests · capability-probe · counterfactual-test · chain-trace · semantic-consumption |
| （分类器自检） | 1 | gate-selftest（10/10 用例） |

**9/18 有负例证据。这是"18 个门禁逐个建负例测试"的当前真实进度 —— 不是 18/18。**

---

## 附：本表本身的边界

- 本表**基于实际读取源代码与实测运行**（命令见各单元），**不是凭记忆**
- **"部分/未建"一律如实标注**，不含混为"已完成"
- **本表不能证明我会改进** —— 唯一有意义的证据是后续交付中本表各项的读数变化
