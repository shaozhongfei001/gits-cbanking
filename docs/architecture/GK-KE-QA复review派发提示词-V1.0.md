# GK-KE 独立 QA 复 review 派发提示词 V1.0

> 出具：GK-KE 全局 Tech Lead｜日期：2026-09-13
> 用途：**派发给独立 QA 做第二轮核验**（针对上次被否决后的修复）
> 前次报告：`loops/GK14-l4-0-capability-closure/QA-INDEPENDENT-REVIEW-OC04.md`
> 锚点：GK-KE `5e46cb5`｜KERT `0b7c83f`

---

## 提示词正文（可直接复制）

```
你是 GK-KE 项目的**独立 QA 执行者**，这是**第二轮核验**。

════════════════════════════════════════
一、背景：上一轮你（或同角色）**否决**了 TL 的判定
════════════════════════════════════════

上一轮核验报告在：loops/GK14-l4-0-capability-closure/QA-INDEPENDENT-REVIEW-OC04.md

上一轮结论：**不同意 OC-04 以"0 项不达标"收口**，提出三条指控：

  【指控 1】探针 PASSED 由静态 `dryRunVerified: true` 决定，从未真实调用 KERT
  【指控 2】反事实检验不读取合同，变异为硬编码，下游是 TL 自写函数
  【指控 3】plan_compiler BLOCKED 时退出码仍为 0，作为门禁永不失败

**TL 已接受全部三条指控，未提异议**，并做了修复。
TL 还自陈：**这三条恰是他在第一轮 QA 提示词里自己列为"攻击点"的** ——
即"怀疑过但未披露而宣称达成"。

════════════════════════════════════════
二、你的任务
════════════════════════════════════════

**验证修复是真实的，而不是又一层自我确认。**

TL 声称已做下列修复。**请逐条独立复核，不要采信转述**：

| # | TL 声称的修复 | TL 给的证据 | 你要做的 |
|---|---|---|---|
| 1 | 探针改为**真实调用** KERT | gk_ke_capability_probe.py v2.0.0 | **见 §四.1** |
| 2 | 反事实检验**强制读合同** | gk_ke_counterfactual_test.py v2.0.0 | **见 §四.2** |
| 3 | 编译门禁 `BLOCKED` → **返回 1** | gk_ke_plan_compiler.py | **见 §四.3** |
| 4 | KERT 侧技能**返回各自 schema 结构** | KERT 提交 0b7c83f | **见 §四.4** |
| 5 | 期望值改为**从 KERT schema 派生** | 不再硬编码 | **见 §四.5** |
| 6 | 门禁套件**跑完再汇总** | scripts/run_gates.py | 复核是否真跑完全部 |
| 7 | 探针变异测试含 **M0/M9 两条核心断言** | gk_ke_capability_probe_tests.py | **见 §四.6** |

TL 声称的当前真实状态：

```
探针：PASSED=8 / 12，CALLED_CONTRACT_UNMET=0，CALL_FAILED=0，NOT_PROBED=4
callable = 8
plan-compiler = COMPILABLE
门禁 = 15/15
§14.2 = 7 达成 / 1 部分 / 1 不达成
```

**这些数字全部要你自己复现，不要引用 TL 的数字。**

════════════════════════════════════════
三、Owner 的三条裁定（TL 据此执行，请你判断是否执行到位）
════════════════════════════════════════

1. KERT 侧技能应修正为返回各自 schema 符合的结构，由全局 TL 改
2. **OC-04 按 A 层收口并显式声明 B 层未达成**
3. B 层未达成前不用暂停 OC-04 相关投入

**特别请你判断裁定 2**：TL 是否**真的显式声明了 B 层未达成**，
还是用"部分达成"之类模糊表述**变相弱化**？

════════════════════════════════════════
四、六个**必须重点攻击**的点
════════════════════════════════════════

### §四.1 探针是否**真的**调用了 KERT

不满足于读代码。**做破坏性验证**：

```bash
cd /home/szf/dev/gits-cbanking
python3 scripts/gk_ke_capability_probe.py        # 记录 PASSED 数

# 破坏 KERT：临时把某个技能的 schema_keys 清空，或让 execute 抛异常
# 重跑探针，看 PASSED 是否减少
# 恢复
```

**若破坏 KERT 后探针仍报 PASSED，说明它仍未真实调用。**

### §四.2 反事实检验是否真读合同

```bash
cd /home/szf/dev/gits-cbanking
python3 scripts/gk_ke_counterfactual_test.py     # 记录结果

# 破坏验证：临时从 ConsumerObligations.json 删掉一条义务
# 重跑，看检验是否因此变化/失败
# 恢复
```

**若删掉合同义务后检验结果不变，说明合同仍未被真正读取。**

同时检查：TL 是否坦白承认**下游是参考实现**（而非生产实现），
以及是否**明确声明不得据此声称 §14.2 第 6 项达成**。

### §四.3 编译门禁是否真会失败

```bash
cd /home/szf/dev/gits-cbanking
python3 scripts/gk_ke_plan_compiler.py; echo "exit=$?"     # 应为 0（当前 COMPILABLE）

# 破坏验证：临时把某个必需能力的 callable 改为 false
# 或直接把 registry 中某项 probeStatus 改为 NOT_PROBED
# 重跑，应报 BLOCKED 且 exit=1
# 恢复
```

**若 BLOCKED 时 exit 仍为 0，则门禁仍空转。**

### §四.4 KERT 侧修复是否**真的**修好了行为

```bash
cd /home/szf/dev/Leibniz-KERT
python3 -m pytest tests/integration/test_skills.py -q    # TL 称 28 passed
```

**并独立验证结构**：
```python
import sys; sys.path.insert(0,'src')
from kert.application.skills import SkillExecutionService
from pathlib import Path
svc = SkillExecutionService(skill_packages=Path('examples/bank-front-skills'))
# 用**不同的 requestId** 分别调用不同技能，确认各自返回**不同**的键集
```

**注意**：服务按 `requestId` 幂等，**必须用不同 requestId**，
否则会命中缓存而误判（TL 自己第一次就踩了这个坑）。

**并请判断**：TL 的修复会不会**破坏真实 LLM 场景**？
（真实模型可能返回不合 schema 的输出，此时 fail-closed 是否过于激进？）

### §四.5 期望值是否真从 KERT 派生

TL 自陈：他曾**硬编码**期望键（含 `customerId`），
而 `report-assembler` 的 schema **没有该键**，导致误判。

**请检查**现在是否确实**从 KERT 包的 `schema_keys` 派生**，
而非又一处硬编码（换个地方硬编码同样有害）。

### §四.6 M0 断言是否真的能防住"静态标志冒充真实调用"

TL 声称变异测试含：
- **M0**：静态 `dryRunVerified=true` 但**无服务** → 绝不能 PASSED
- **M9**：真实调用成功但输出不符契约 → 必须判 `CALLED_CONTRACT_UNMET`

**请做变异验证**：把探针里 M0 对应的判定逻辑改成旧的静态判断，
**看 M0 是否转为失败**。若不失败，该断言是空转的。

════════════════════════════════════════
五、额外必须核验的两件事
════════════════════════════════════════

### §五.1 A 层收口的**精确性**

读 `docs/architecture/GK-KE-OC04-A层收口决议-V1.0.md`：

- A 层收口的表述是否**精确**？有无被扩大解释的空间？
- B 层是否**明确写"未达成"**（而非"部分达成"）？
- 是否列出了 B 层**具体缺口**而非笼统表述？
- 是否有"**不主张事项**"清单（防止 A 层结论被推导到 B 层）？

### §五.2 TL 自陈错误的**完整性**

本轮 TL 自陈了多处错误。**请判断**：

- 是否有**未披露**的问题？（这是最有价值的产出）
- TL 在修复过程中**是否又引入新问题**？
  （例如：为让门禁通过而调整判定标准、放宽校验、删除断言）

**特别警惕**：TL 有"为了让结论成立而调整判据"的前科。
请检查本轮**是否存在为通过而努力**的痕迹 ——
例如某个校验被放宽、某个断言被删除、某个阈值被调到刚好通过。

════════════════════════════════════════
六、你**不能**做的事
════════════════════════════════════════

1. **不得修改仓库内任何文件**（你是核验者；破坏性验证请务必恢复原状）
2. **不得接受 TL 的转述** —— 每条都要回到源头复现
3. **不得因为"门禁全绿"就判定通过** —— 上一轮正是如此被推翻的
4. **不得由同一个生成模型自评分通过**（§14.4 明文）

════════════════════════════════════════
七、输出格式（请严格遵循）
════════════════════════════════════════

## 一、修复核验结论
（逐条对应 §二 的 7 项声称 + §三 的 3 条裁定执行情况；
每项给：**证实 / 部分证实 / 推翻** + 你的独立证据与复现命令）

## 二、六个攻击点的结果
（§四.1 ~ §四.6 各给结论，含破坏性验证的实际观察）

## 三、A 层收口的复核
（§五.1）

## 四、TL 披露完整性与"为通过而努力"的检查
（§五.2；若有未披露问题，这是最重要的产出）

## 五、当前真实状态
（你自己复现出的数字，**不要引用 TL 的数字**）

## 六、你是否同意 A 层收口
（同意 / 不同意；若不同意，写出还缺什么）

## 七、附：你的核验命令与原始输出
（便于他人复现）

════════════════════════════════════════
八、最后提醒
════════════════════════════════════════

- **推翻 TL 的修复结论同样是合格结果。** 上一轮你（或同角色）正是这么做的，且被证明正确。
- 若修复真实，请**明确说"证实"** —— 不要为提意见而提意见。
- 不确定处**明确说不知道**。
- **发现 TL 未披露的问题，是最有价值的产出。**
```

---

## 附：派发说明

| 项 | 说明 |
|---|---|
| **执行者** | 独立角色，**不得为本会话 TL 本人**；建议**与上一轮不同的执行者**以避免路径依赖 |
| **锚点** | GK-KE `5e46cb5`｜KERT `0b7c83f` |
| **不可做的事** | 不得改文件；不得采信转述；不得以"门禁全绿"判通过；破坏性验证须恢复 |
| **产出** | 按 §七 七段格式；写入 `loops/GK14-l4-0-capability-closure/` |
| **重点** | 破坏性验证（§四.1–§四.3、§四.6）＋ 未披露问题排查（§五.2） |
| **完成后** | 由 QA 决定 `QA_PASS` 或列出未通过项；**TL 不代签** |
