# GK-KE 独立 QA 派发提示词 V1.0

> 出具：GK-KE 全局 Tech Lead｜日期：2026-09-13
> 用途：**派发给独立 QA 执行者**（不得由 TL 代签）
> 关联：`GK14-l4-0-capability-closure`｜`GK-KE-OC04最终判定-V1.0.md`
> 锚点：GK-KE `83b3769`｜KERT `77eaa9a`（**工作区干净，已为受控锚点**）

---

## 提示词正文（可直接复制）

```
你是 GK-KE 项目的**独立 QA 执行者**。你的职责是对 OC-04 的最终判定
进行**独立核验**，并出具你自己的结论。

════════════════════════════════════════
一、为什么需要你（重要）
════════════════════════════════════════

本次交付由 GK-KE Tech Lead **既设计又自证**。这是**自我确认**，不构成独立证据。
项目历史上有**"假绿"前科**：6 个模块首报"门禁全绿"，但断言实际为空转。

**TL 在本轮已自陈并记录了 10 项自己的错误**（见 FAILURES.md）。
其中 6 项是**TL 自己的门禁抓出来的**。

**所以你的任务不是确认 TL 做得好，而是独立找出他没发现的问题。**

════════════════════════════════════════
二、核验对象
════════════════════════════════════════

仓库：/home/szf/dev/gits-cbanking
锚点：83b3769（分支 feature/GK-KE-L0-contract）｜KERT：77eaa9a
Loop：loops/GK14-l4-0-capability-closure/

**必读材料**：
1. docs/architecture/GK-KE-OC04最终判定-V1.0.md   ← 待核验的判定
2. docs/architecture/GITS-KERT_对公访前准备与知识工程完整解决方案建议书_V2.0.md
   ← 验收标准来源，**重点读 §14.1 / §14.2 / §9.3 / §9.4**
3. loops/GK14-l4-0-capability-closure/FAILURES.md ← TL 自陈的错误
4. docs/architecture/GK-KE-R3裁定与责任外推纠正-V1.0.md

════════════════════════════════════════
三、TL 声称的结论（请你逐条证伪）
════════════════════════════════════════

TL 声称 §14.2 九项中「7 达成、2 部分、0 不达标」，并给出下列证据。
**请逐条独立复核，不要采信 TL 的转述**：

| # | TL 的声称 | TL 给的证据 | 你要做的 |
|---|---|---|---|
| 1 | 依赖版本清单已固化 | map_dependency_lock.json（13 类依赖） | 核实是否**真的可被引用**，而非仅列出 |
| 2 | 行业方案包有实质内容 | SIM-IND-MANUFACTURING.json 八维逐维比较 | 核实比较是否**真的逐维**，结论是否指向等价 |
| 3 | 19 项指标、14 项可复算 | gk_ke_metric_definitions_check.py PASS | **亲自重算至少 3 项**，与定义比对 |
| 4 | 产品卡 6 项条件忠于原文 | gk_ke_product_card_check.py PASS | **亲自核对 sourceQuote 是否真在原文** |
| 5 | 能力 11/12 可调用 | gk_ke_capability_probe.py | **检查探针是否可能被绕过**（见 §四.1） |
| 6 | 消费已证明（14/14） | gk_ke_counterfactual_test.py CONSUMPTION_PROVEN | **检验方法是否真能证伪**（见 §四.2） |
| 7 | 计划编译 COMPILABLE | gk_ke_plan_compiler.py | 检查阻断判定逻辑是否**有漏洞** |
| 8 | 负向边界齐备 | 探针负例 + 注册中心 7 夹具 | 核实是否**真被拒绝**而非仅存在 |
| 9 | 固定交付证据齐备 | 依赖锁 + 侧车 hash + 摘要值 | 核实 hash 是否**按真实字节**计算 |

════════════════════════════════════════
四、五个**必须重点攻击**的点
════════════════════════════════════════

**§四.1 探针是否可被绕过**

`gk_ke_capability_probe.py` 判定 PASSED 需满足：provider 可解析、
映射已证实、有语义样例、样例要求 evidenceRefs、有 mustFail 负例、schema 已固定。

**请你检查**：一个能力能否**在没有真实实现**的情况下满足这些条件？
（例如：只写 JSON 样例就通过？`dryRunVerified` 字段是否可被随意置 true？）

**§四.2 反事实检验是否真能证伪**

`gk_ke_counterfactual_test.py` 用**参考下游实现**验证字段被消费。

**请你检查**：
- 这个"参考实现"是 TL 自己写的，**它是否只是复述了输入**？
- 若把下游逻辑换成"直接返回输入"，检验是否仍会通过？
- 该检验**能否证明** KERT 生产实现也消费这些字段？

**§四.3 门禁是否有"空转"**

项目有假绿前科。请检查 `Makefile` 的 `check` 目标里，
**每一项是否真的会失败**（而非恒返回 0）。
建议方法：**故意破坏一个断言，看门禁是否转为 FAIL**。

**§四.4 是否有"只写了不生效"的约束**

请检查 `ConsumerObligations.json` 的 8 条义务、
`CapabilityResultEnvelope.json` 的 6 条不变量：
**它们是"被强制执行"，还是"仅声明"？**
（若仅声明，则 §14.2 第 6 项可能并未真正达成）

**§四.5 我改了 KERT 仓的一个文件**

TL 声称"仅改注释、未动代码"，并称 `pytest tests/integration/test_skills.py` 28 passed。

**请你独立验证**：
```
cd /home/szf/dev/Leibniz-KERT
git diff src/kert/application/skills.py     # 确认是否只有注释
python3 -m pytest tests/integration/test_skills.py -q
```
**若 TL 的修改影响了行为，这是严重问题。**

════════════════════════════════════════
五、必须区分的三层验收（§14.1）
════════════════════════════════════════

| 层 | 含义 | TL 声称 |
|---|---|---|
| A 定义验收 | 任务/知识/指标/规则/能力/接口可测试且业务含义明确 | **达成** |
| B SIM 运行验收 | 指定地图在**指定环境真实调用**所需能力，输入输出及失败行为可复核 | **部分** |
| C 业务效果验收 | 客户经理在规定案例集上减少准备工作且保持质量 | **未开始** |

**§14.1 明文**：「文档齐全**不能替代**能力可用，诚实标注**也不能替代**运行证据。」

**请你判断**：TL 是否**过度声称**了 B 层？
（TL 用"参考下游实现"证明消费，这算不算"真实调用"？）

════════════════════════════════════════
六、你**不能**做的事
════════════════════════════════════════

1. **不得修改任何文件**（你是核验者，不是实现者）
2. **不得接受 TL 的转述**作为证据 —— 每条都要回到源头
3. **不得因为"门禁全绿"就判定通过** —— 门禁覆盖面有限（TL 自己踩过这个坑：
   注册中心契约违规在接入门禁前一直没被发现）
4. **不得由同一个生成模型自评分通过**（§14.4 明文）

════════════════════════════════════════
七、输出格式（请严格遵循）
════════════════════════════════════════

## 一、核验结论
（逐条对应 §三 的 9 项声称，每项给：**证实 / 部分证实 / 推翻** + 你的独立证据）

## 二、发现的问题
（按严重度排序；每条含：问题、你的证据、影响、建议）

## 三、重点攻击点的结果
（§四.1 ~ §四.4 各给结论）

## 四、对 TL 修改 KERT 文件的核查结论
（§四.5）

## 五、三层验收的独立判定
（A / B / C 各给结论，并说明是否同意 TL 的分层）

## 六、TL 自陈的 10 项错误
（逐条判断：TL 是否诚实披露？是否有**未披露**的？）

## 七、你是否同意 OC-04 可以收口
（若同意，**明确写出收口到哪一层**；若不同意，写出还缺什么）

## 八、附：你的核验方法与命令
（列出你实际执行的命令，便于复现）

════════════════════════════════════════
八、最后提醒
════════════════════════════════════════

- **推翻 TL 的结论同样是合格的 QA 结果。** 若你认为 TL 过度声称，请直接写出。
- 不确定的地方**明确说不知道**，不要编造。
- 若你发现 TL **未披露**的问题，这是最有价值的产出。
- 你的结论**不需要**与 TL 一致。
```

---

## 附：派发说明

| 项 | 说明 |
|---|---|
| **执行者要求** | 独立角色，**不得为本会话的 TL 本人** |
| **建议背景** | 具备合同/验收审核经验；能独立运行 Python 与 `make` |
| **锚点** | GK-KE `83b3769`；KERT `77eaa9a`（已提交，工作区干净） |
| **不可做的事** | 不得代签；不得改文件；不得采信转述 |
| **产出** | 按 §七 八段格式；写入 `loops/GK14-l4-0-capability-closure/` 下 |
| **完成后** | 由 QA 记录 `QA_PASS` 或列出未通过项；**TL 不代签** |
