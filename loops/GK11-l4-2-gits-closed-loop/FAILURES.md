# GK11-l4-2-gits-closed-loop｜Failures（append-only）

## FAIL-2026-09-12-14: L4-2 六类拒绝断言全部空转（独立 QA 变异测试发现）

- **时间**：2026-09-12（Wave E，Independent QA 审计）
- **Gate**：`l4_2_closed_loop_tests`
- **命令**：变异测试 —— 同时禁用「禁止动作」与「超时查回执」检查后重跑
- **退出码**：0（**期望非 0**）
- **分类**：**MAJOR**（C08 L4-2 四项退出标准中的「失效版本」「超时对账」未真实验证）
- **现象**：禁用两类检查后测试**仍 PASS**。
- **根因**：与 FAIL-2026-09-12-02 / -07 / -11 / -12 **同一缺陷模式**：
  全部夹具都是**合规正例**（动作在白名单内、超时已查询回执、版本未失效、
  声明未被当事实、用途未知已判 UNKNOWN），故**所有拒绝分支从未进入**。
  `violations` 段只是**声明式**数据（`detected: true` 是自述），**未被执行**。
- **合规影响**：C08 L4-2 退出标准为「3000 万场景、**UNKNOWN**、**失效版本**、**超时对账**」——
  后三项本质是否定式要求，只测正例无法证明。
  且「白名单强制」（OWNER-003 §6.1：禁止转账/放款/授信审批）是**安全红线**，
  其断言空转意味着红线未被验证。
- **下一动作**：把 `violations` 从声明式改为**可执行夹具**，
  用同一套判定函数对其求真值（fail-closed）；缺夹具即失败。

### 修复记录（第 1 轮，已闭环）

- **修复**：引入 `detect_violations(doc)` 统一判定函数（与 L3-2 同构），
  `violations` 改为**可执行文档**（含真实字段而非自述 `detected`）；
  测试对每个违规文档求真值，要求触发对应错误码；
  并断言 6 类拒绝路径**全部**有夹具（缺一即失败）。
- **验证（变异必须失败）**：再次禁用禁止动作/超时检查 → 测试 **FAIL**。
- **验证（恢复必须通过）**：恢复 → 测试 **PASS**（正例通过 + 6 类违规全被检出）。
- **状态**：CLOSED（1 轮）。
