# GK16-trust-hardening｜Next Session Baton

| 字段 | 值 |
|---|---|
| **Updated** | 2026-09-13（第 9 代收工） |
| **holder** | `tech_lead` |
| **packet** | `GK16-trust-hardening` |
| **wave** | `W9` |
| **status** | `in_progress`（**未收敛**） |
| **do_not_start** | QA_PASS、REAL_E2E_PASS、BUSINESS_SIGNED |
| **上一代** | `tech_lead`（第 8 代，交接文档 `docs/architecture/GK-KE-TL交接文档-V1.0.md`） |

---

## 一句话状态

> **本仓赖以证明「下游消费上游」的那条链路，其「消费」在链路上从未成立。**
> 机制先**完全死**（T-28），修好后暴露**词汇不匹配**（T-30）；
> 而门禁 `chain-trace` 对这两件事**全程无感**（T-29）。
> **独立判定 `semantic-consumption = NOT_MET` 在此获得一条独立的机制性佐证 —— TL 不代判、不改该判定。**

---

## 本代做了什么（全部附可复现命令）

| # | 项 | 状态 | 证据 |
|---|---|---|---|
| T-22 | KERT 跨仓工作区不干净（交接 §六 第 8 条不符） | **已修** | KERT `5708ef4` |
| T-23 | 门禁套件每次运行弄脏受版本控制的证据文件（含运行时时间戳） | 已登记，未修 | `FAILURES.md` |
| T-24 | 并行运行门禁 ⇒ 我方测量污染（假 FAIL / 假"合同被改"） | **未确立**（不得当缺陷） | `FAILURES.md` |
| T-25 | 派发提示词早于新观测 ⇒ item #1 在**构造上**无法完成 | **已修** | V1.1 提示词 + V1.0 作废横幅 |
| T-26 | `chain-trace`「输入消费证明」与其反事实输出不同向 | 已升级为 T-28/T-29 | `FAILURES.md` |
| T-27 | T-11 哈希失配复核 | 不复现，维持"未解释" | 5/5 锚点 MATCH |
| T-28 | 下游受控枚举**恒取默认值**，"再入式取值"从未生效 | **已修 + 负例自证** | KERT `c65a861` |
| T-29 | `chain-trace` 用"输入变化"冒充"消费"，反证藏在 JSON 里 | **已修**（仅改打印） | `scripts/gk_ke_chain_trace.py` |
| T-30 | 映射层与下游技能输入契约**词汇不匹配** | **已登记/未修**（BLOCKER） | `FAILURES.md` |
| T-31 | `EVIDENCE.json` 摘要与其锚定证据文件**矛盾** | 已刷新（未加防线） | `EVIDENCE.json` |

**KERT 仓两个提交**：`5708ef4`（忽略 UA 派生制品、UA 规则文件入库）、`c65a861`（根因修复 + 负例测试）。

---

## 下一步队列（**按序，不得跳**）

1. **T-30**（BLOCKER）映射层与下游技能输入契约词汇不匹配 ——
   需先裁定**桥接放哪**：映射合同里 vs 独立的下游输入适配器；
   属**合同先行**变更（改合同源 → `make generate` → `make check` → 再改实现），
   且 `chain-trace` 自带三条机械校验会同时约束。
2. **T-31** 为「摘要 vs 证据文件」加**机械一致性防线** ——
   新增门禁**必须附负例测试**（硬约束 #2/#3），否则它就是新的"看起来有防线"。
3. **T-29 后续** 是否把 `output_consumed` 纳入 `chain-trace` 的 pass 条件 ——
   **须待 ① 完成**，否则会因词汇不匹配而**错误变红**。
4. 把 **V1.1 提示词**交给**另一名独立执行者**做语义消费复判（TL 不得代判）。
5. 判据 V1.2.1 的两项档案缺口（预注册零引证、无 §观测）→ **判据作者侧**，TL 不得改判据内容。

---

## 纪律（违反即重演本次错误）

1. **门禁必须串行运行**（会写盘）；并行 = 污染自己的测量（T-24 的代价）。
2. **读代码只生成假设**：本代我的第一假设被实测证伪，真实根因更严重（T-28）。
3. **"移除字段"不足以证明消费**：默认值恰等于真实值时不可分辨，须注入**同枚举他值**。
4. **不得静默降级**：`except Exception` + `debug` 把 T-28 掩盖了一整代。
5. **记录类文件只追加**（T-21）；本代 `FAILURES.md` 由 429 行追加至 673 行，T-01~T-21 无损。
6. **不得改判据内容、不得代判 `semantic-consumption`、不得放开只读制品写权限。**
